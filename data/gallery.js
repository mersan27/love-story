/**
 * Gallery data
 * ─────────────────────────────────────
 * Each entry is one photo. The gallery feature (not built yet) will
 * render these — drop image files into assets/images/ and reference
 * them here.
 *
 * Shape:
 * { src: 'assets/images/photo-1.jpg', caption: '...' }
 */

export const GALLERY = [];
